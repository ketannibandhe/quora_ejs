<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Quora Posts</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <div class="container">
      <h1 class="heading">Quora Posts</h1>
        <% posts.forEach(post => { %>
        <div class="post">
          <div class="username">@<%= post.username %></div>
          <div class="content"><%= post.content %></div>
          <div style="display: flex;">
          <a href="/posts/<%=post.id%>"><button type="button" style="width: fit-content;margin-left: 10px;">see in detail</button></a>
          <a href="/posts/<%=post.id%>/edit"><button type="button" style="width: fit-content;margin-left: 10px;">edit post</button></a>

          <form  method="post" action="/posts/<%= post.id %>?_method=DELETE">
            <button type="submit" style="width: fit-content;margin-left: 10px;">delete post</button>
          </form>
          </div>
        </div>
        <% }) %>
    </div>
    <a href="/new"><button type="button" style="width: fit-content;margin: auto;">Add Post</button></a>
  </body>
</html>
